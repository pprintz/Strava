behavior onSetup():
    //valStruct {num value}
    //valStruct myNext := valStruct[value := 10]

    //myStruct {valStruct next, num value }
    //myStruct head := myStruct[next := myNext, value := 2]

    myStruct {num a, num b}

    myStruct lo := myStruct[b := 123, a := 2]

    num tal := lo.a
;
strategy default:
;
