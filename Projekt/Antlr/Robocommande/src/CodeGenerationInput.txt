//behavior onSetup () :

//;

strategy default:
	behavior onRun () :

    ;

    behavior onHitByBullet(hitByBulletEvent e) :
        num a := 5
        text b := "Hello, world!"
        bool c := false
    ;
;

strategy aggressive :
	behavior onRun () :
		num a := 1
		bool b
		text c
	;

	behavior onHitByBullet(hitByBulletEvent e):

	;

	behavior onScannedRobot(scannedRobotEvent e) :

	;
;

define text ds() :

;


// rotate
// move
// changeStrat
